//
//  ContentView.swift
//  OpenSlipREAL
//
//  Created by Emeline Mannix on 3/30/26.
//

import SwiftUI


struct ContentView: View {
    var body: some View {
        HStack {
            DayForecast(day: "Mon", high: 70, low: 50)
            
            DayForecast(day: "Tue", high: 60, low: 40)
            
            Name(yourname)
        }
    }
}


struct DayForecast: View {
    let day: String
    let high: Int
    let low: Int
    
    var body: some View {
        VStack {
            Text(day)
            Image(systemName: "sun.max.fill")
                .foregroundStyle(Color.yellow)
            Text("High: 70")
            Text("Low: 50")
        }
        .padding()
    }
}

struct Name: View {
    let yourname: Int
   
   var body: some View {
        VStack {
            TextField("What is your name", value: yourname, format: .number)
        }
    }


}


#Preview {
    ContentView()
}
